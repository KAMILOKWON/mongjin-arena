{
  "name": "@mongjin/arena",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify:product": "tsx scripts/verify-product-equivalence.ts",
    "experiment:first-move": "tsx scripts/run-subscription-first-move.ts",
    "arena": "tsx src/cli/arena.ts",
    "match": "tsx src/cli/match.ts"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
